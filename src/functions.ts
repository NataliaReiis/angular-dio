function addNumber( x: number, y:number): number {
    return x + y
}
let soma:number = addNumber(4, 5)
console.log(soma)

///////////////////////////////////////////////////

function CallTophone(phone:number | string) : any {
    return phone
}
let somaa: number = addNumber(4, 7);
console.log(somaa)

/* Função async */

async function getDatabase(id: number): Promise<string> { 
    return "aiaiaia"
}